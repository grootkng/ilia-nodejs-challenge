import * as schemas from './schemas/'

export default {
  userWithoutPasswordSchema: schemas.userWithoutPasswordSchema,
  usersSchema: schemas.userSchema,
  userSchema: schemas.userSchema,

  loginSchema: schemas.loginSchema
}
